apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-studio-charts.fullname" . }}
  labels:
    {{- include "ai-studio-charts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-studio-charts.selectorLabels" . | nindent 4 }}
