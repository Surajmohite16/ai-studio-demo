apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-studio-charts.fullname" . }}-test-connection"
  labels:
    {{- include "ai-studio-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-studio-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
